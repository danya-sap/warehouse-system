# 📦 Warehouse Management System (WMS) Pro

<p align="center">
  <img src="https://nestjs.com/img/logo-small.svg" width="60" alt="Nest Logo" />
  <img src="https://upload.wikimedia.org/wikipedia/commons/a/a7/React-icon.svg" width="60" alt="React Logo" />
</p>

Система управления складскими остатками с реализацией стратегии **FEFO** (First Expired, First Out).

---

## 🌟 Ключевая особенность: FEFO
В отличие от обычных складских систем, данный проект реализует логику **FEFO**. 
При списании товара система анализирует все имеющиеся партии и автоматически предлагает к отгрузке те товары, срок годности которых истекает в ближайшее время. Это минимизирует убытки от порчи продукции.

## 🛠 Технологический стек
* **Backend**: NestJS (TypeScript), Prisma ORM
* **Frontend**: React, Tailwind CSS
* **Database**: PostgreSQL 16
* **Infrastructure**: Docker, Docker Compose

---

## 🚀 Как запустить проект

Проект полностью контейнеризирован. Вам не нужно устанавливать Node.js или базу данных локально.

1.  **Запуск всех сервисов**:
    ```bash
    docker-compose up --build -d
    ```

2.  **Настройка базы данных** (выполните один раз после запуска):
    ```bash
    docker exec -it wms-backend npx prisma migrate deploy
    ```

3.  **Доступ к системе**:
    * **Frontend**: [http://localhost](http://localhost) (Порт 80)
    * **Backend API**: [http://localhost:3000/api](http://localhost:3000/api)

---

## 🔑 Доступы для тестирования
После запуска база данных автоматически наполняется (Seeding) следующими пользователями:

| Роль | Email | Пароль |
| :--- | :--- | :--- |
| **Администратор** | `superadmin@warehouse.com` | `admin123` |
| **Менеджер** | `manager@warehouse.com` | `admin123` |
| **Работник склада** | `worker@warehouse.com` | `admin123` |

---

## 📁 Структура проекта
* `/backend` - Исходный код сервера (NestJS)
* `/frontend` - Исходный код клиентской части (React)
* `docker-compose.yml` - Конфигурация всей инфраструктуры